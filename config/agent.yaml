agent:
  mode: human

  gate:
    approve:
      - just fmt
    auto:
      - just fmt

  auto:
    allow_risk:
      - low
    require_check: true
    allow_rule_change: false
    allow_workflow_change: false
    allow_arch_change: false
