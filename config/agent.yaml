backend:
  active: omo
session:
  logging:
    message: redacted
  vector:
    backend: qdrant-local
    collection: agent-session
    failure: fail
    heartbeat: true
    model: sentence-transformers/all-MiniLM-L6-v2
    required: true
